# Session Start Anchor

- Start with `AGENT.md`, `CLAUDE.md`, and `README.md`.
- Run `python3 scripts/work.py resume` once PROMPT_05 has provided it.
- Read the briefing printed by the resume command.
- Read `memory/INDEX.md`.
- Read relevant `memory/concepts/` artifacts only after identifying the task.
- Infer the task before loading stack detail.
- Prefer one router, one workflow, one stack, one archetype, and one example.
- Stop when ambiguity would force multiple primary bundles.
