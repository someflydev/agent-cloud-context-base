apiVersion: batch/v1
kind: Job
metadata:
  name: multi-role-platform-replay
  namespace: accb-eks-platform
spec:
  template:
    spec:
      serviceAccountName: multi-role-platform
      restartPolicy: Never
      containers:
        - name: job
          image: ghcr.io/someflydev/accb-canonical-eks-job:dev
          env:
            - name: ACCB_ENV
              value: dev
