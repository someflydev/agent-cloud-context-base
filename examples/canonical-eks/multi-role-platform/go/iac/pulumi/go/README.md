# Pulumi Go EKS Control Plane

This tree models the EKS cluster, IRSA workload identity, MSK, S3, and DynamoDB control-plane resources for the Go multi-role platform. Dev and test stacks use disjoint stack names, regions, secret paths, and `accb-aws-$env-multi-role-platform` resource names.
