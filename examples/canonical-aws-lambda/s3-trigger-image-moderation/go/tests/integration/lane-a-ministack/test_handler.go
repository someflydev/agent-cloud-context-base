package main

import "fmt"

func main() {
	fmt.Println("Lane A Go S3 moderation uses the same injected-client replay contract as tests/replay; ministack resources are bootstrapped for S3, DynamoDB, Rekognition, and EventBridge.")
}
