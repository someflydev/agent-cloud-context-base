You are adding the first dev/test IaC isolation pair to a repo generated by
`accb`.

Before generating resources, declare:

- Dev state key or prefix
- Test state key or prefix
- Dev env-var prefix and secret path
- Test env-var prefix and secret path
- Resource naming template including environment

Create `iac/<tool>/dev` and `iac/<tool>/test` or the equivalent Pulumi
`Pulumi.dev.yaml` and `Pulumi.test.yaml` stack files.

Run:

```sh
python3 scripts/validate_iac_isolation.py <iac-root>
```

Do not claim completion until validation is clean or the blocker is documented.
