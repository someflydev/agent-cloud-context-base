# Pulumi Go GKE Control Plane

This tree models the GKE Autopilot cluster, Workload Identity, GCS, AlloyDB, and Vertex Vector Search control-plane resources for the Go multi-role platform. Dev and test stacks use disjoint stack names, regions, secret paths, and `accb-gcp-$env-multi-role-platform` resource names.
