# Pulumi TypeScript AKS Control Plane

This tree models the AKS cluster, Azure Workload Identity, Cosmos DB, Azure SQL, and Service Bus control-plane resources for the TypeScript multi-role platform. Dev and test stacks use disjoint stack names, regions, secret paths, and `accb-azure-$env-multi-role-platform` resource names.
