# Structured Log Shape

Required fields: `timestamp`, `trace_id`, `provider=azure`,
`service=azure-functions`, `example=blob-trigger-receipt-ocr`,
`idempotency_key`, `blob_name`, `blob_version`, `cosmos_status`,
`event_grid_status`, `duration_ms`.
