# Structured Log Shape

Required fields: `timestamp`, `trace_id`, `provider=azure`,
`service=azure-functions`, `example=cosmos-changefeed-search-sync`,
`cosmos_id`, `_etag`, `idempotency_key`, `search_index_status`,
`retry_queue_status`, `duration_ms`.
