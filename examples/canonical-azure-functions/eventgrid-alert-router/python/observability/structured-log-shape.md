# Structured Log Shape

Required fields: `timestamp`, `trace_id`, `provider=azure`,
`service=azure-functions`, `example=eventgrid-alert-router`,
`idempotency_key`, `event_id`, `resource_id`, `severity`, `team`,
`cosmos_status`, `service_bus_topic`, `duration_ms`.
