You are working inside the `agent-cloud-context-base` repository (short name:
**accb**).

This is PROMPT_27 of 33. PROMPT_20 authored the Azure Functions seed set.
This prompt completes the first-class Azure Functions canonical surface under
`examples/canonical-azure-functions/` by adding the remaining required
examples that were deferred from the seed pass.

Use **`accb`** / **`.accb/`** in everything; never `acb` / `.acb/`.

   [CLOUD ARC: Prompt 27 of 33 — Canonical Azure Functions Expansion]

================================================================================
SESSION EXECUTION DISCIPLINE
================================================================================

One prompt per session. Maintain `tmp/PROMPT_27_checklist.md`.

================================================================================
SHARED CONTEXT BRIEFING — agent-cloud-context-base (accb)
================================================================================

Reuse the PROMPT_20 contract exactly. The layout, Azurite lane, Lane B shape,
replay expectations, observability expectations, and registry/catalog rules
already exist.

Sequence:
  PROMPT_01–26  Through GCP Cloud Functions expansion                    (done)
  PROMPT_27     Canonical Azure Functions expansion                      (this prompt)
  PROMPT_28     Canonical Cloud Run + App Runner expansion
  PROMPT_29     Canonical Container Apps expansion
  PROMPT_30     Canonical EKS + GKE + AKS expansion
  PROMPT_31     Canonical Pulumi starter expansion
  PROMPT_32     Integration testing harness + arc docs
  PROMPT_33     Final wiring

================================================================================
BOOT SEQUENCE
================================================================================

Read:

  1. `README.md`, `AGENT.md`
  2. `python3 scripts/work.py resume`
  3. `.prompts/PROMPT_20.txt` (FULL READ — layout + lane contract)
  4. `examples/canonical-azure-functions/` (all seed examples)
  5. `verification/example_registry.yaml`
  6. `examples/catalog.json`
  7. `verification/stack_support_matrix.yaml`
  8. `context/stacks/azure-fn-{python,typescript-node,dotnet-isolated}.md`
  9. `templates/function/azure-fn/`, `templates/iac/{terraform,pulumi/*}/azure/`

================================================================================
PART 1 — REUSE PROMPT_20 VERBATIM
================================================================================

Reuse PROMPT_20's contract verbatim:

  - same directory layout
  - same Azurite-oriented Lane A rules
  - same Lane B ephemeral-real structure
  - same replay and observability rules inherited from PROMPT_18
  - same registry/catalog entry shape

================================================================================
PART 2 — REQUIRED AZURE EXPANSION EXAMPLES (3 examples)
================================================================================

Author the remaining required examples:

  2.1  blob-trigger-receipt-ocr/typescript/
  2.2  blob-trigger-receipt-ocr/dotnet-isolated/
  2.3  eventgrid-alert-router/python/

Keep the workflows and intent exactly as described in PROMPT_20's original
Part 2 descriptions. This prompt only restores the deferred required surface.

================================================================================
PART 3 — REGISTRY + CATALOG + SUPPORT MATRIX
================================================================================

Append the 3 new entries to `verification/example_registry.yaml` and
`examples/catalog.json` using the PROMPT_20 / PROMPT_18 entry shapes.

Update `verification/stack_support_matrix.yaml` so Azure Functions now has
the full first-class surface originally promised in PROMPT_20:

  - python, typescript, dotnet-isolated are SUPPORTED with full example coverage
  - powershell remains DEFERRED for ops-only cases

================================================================================
VERIFICATION (this prompt)
================================================================================

  1. `find examples/canonical-azure-functions -mindepth 2 -maxdepth 2 -type d | wc -l`
     shows 6 example directories.
  2. Every Azure Functions example has the PROMPT_18 Part 1 layout with the
     PROMPT_20 provider substitutions.
  3. `python3 scripts/validate_iac_isolation.py` is clean for every new
     example's Terraform and Pulumi tree.
  4. `python3 scripts/verify_examples.py --family canonical-azure-functions`
     runs without crashing.
  5. Catalog + registry + support matrix reflect the full 6-example surface.
  6. `grep -rIn --exclude-dir=.git -E '\bacb\b|\.acb/' examples/canonical-azure-functions/`
     returns nothing.

================================================================================
COMPLETION GATES
================================================================================

You may edit this file in place to tick the boxes off after the prompt has
been run.

  - [ ] All 6 Azure Functions examples now exist with the established layout.
  - [ ] The 3 deferred required examples from PROMPT_20 are now authored.
  - [ ] Every Azure example has replay coverage and declared structured logs.
  - [ ] Terraform + Pulumi-language IaC for every Azure example is
        `validate_iac_isolation` clean.
  - [ ] Catalog + registry + support matrix reflect the full Azure surface.
  - [ ] No `acb` / `.acb/` references.
  - [ ] `tmp/PROMPT_27_checklist.md` is fully checked.
  - [ ] `memory/concepts/azure-functions-canonical-arc.md` is updated to note
        the full 6-example surface and that PROMPT_28 continues the managed-
        container expansion.
  - [ ] `memory/INDEX.md` lists the updated concept.

End of PROMPT_27.
