# Scenario Catalog System

PROMPT_07 distilled the sibling excellent-cloud prompt catalogs into `context/scenarios/` as workload taxonomies rather than copied prompt dumps. The function, managed-container, and Kubernetes scenario files plus `scenario-profile-map.yaml` translate recurring business-shaped cloud requests into `accb` archetype, runtime-tier, provider, support-service, manifest, canonical-example, and validation choices while preserving a no-copying rule for the original catalogs.
