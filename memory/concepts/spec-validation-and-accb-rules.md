# Spec Validation And accb Rules

PROMPT_04 authored the canonical spec narratives in `context/specs/`, the
validation narratives in `context/validation/`, and the machine-readable
composition rules in `context/accb/profile-rules.json`. These artifacts define
the baseline product, architecture, agent, evolution, validation, archetype,
stack, capability, and gate metadata that PROMPT_16 wired into generated
`.accb/` payload composition.

PROMPT_05 implemented `scripts/work.py` and the memory layer, establishing
the runtime continuity loop used by later prompt sessions.
