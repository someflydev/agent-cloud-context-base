schema_version: 1
matrix:
  aws:
    managed_container:
      app-runner:
        python-fastapi:
          status: supported
          support_level: first-class-example
          family: canonical-app-runner
          example: public-api-with-vpc-connector
        go-echo:
          status: deferred
          reason: App Runner Go Echo variant is deferred to PROMPT_28
        supplier-onboarding:
          status: deferred
          reason: App Runner supplier onboarding workflow is deferred to PROMPT_28
    function:
      aws-lambda:
        python:
          status: supported
          support_level: first-class-example
          family: canonical-aws-lambda
          example: s3-trigger-image-moderation
        typescript:
          status: supported
          support_level: first-class-example
          family: canonical-aws-lambda
          example: apigw-stripe-webhook
        go:
          status: supported
          support_level: first-class-example
          family: canonical-aws-lambda
          example: sqs-document-translation
        java:
          status: deferred
          reason: rare JVM-oriented Lambda cases are outside this arc's seed pass
        dotnet:
          status: not-planned
          reason: not planned for the canonical AWS Lambda arc
        ruby:
          status: not-planned
          reason: not planned for the canonical AWS Lambda arc
  gcp:
    managed_container:
      cloud-run:
        python-fastapi:
          status: supported
          support_level: first-class-example
          family: canonical-cloud-run
          example: public-api-private-worker-job
        multi-container-sidecar:
          status: supported
          support_level: first-class-example
          family: canonical-cloud-run
          example: multi-container-sidecar
        go-echo:
          status: deferred
          reason: Cloud Run Go Echo variant is deferred to PROMPT_28
        typescript-hono:
          status: deferred
          reason: Cloud Run TypeScript Hono variant is deferred to PROMPT_28
        cloudrun-job:
          status: deferred
          reason: standalone Cloud Run job example is deferred to PROMPT_28
    function:
      gcp-cloud-functions:
        python:
          status: supported
          support_level: first-class-example
          family: canonical-gcp-functions
          example: gcs-trigger-ocr-to-firestore
        typescript:
          status: supported
          support_level: first-class-example
          family: canonical-gcp-functions
          example: http-stripe-webhook
        go:
          status: supported
          support_level: first-class-example
          family: canonical-gcp-functions
          example: eventarc-monitoring-router
        java:
          status: deferred
          reason: rare JVM-oriented Cloud Functions cases are outside this arc's seed pass
        dotnet:
          status: not-planned
          reason: not planned for the canonical GCP Cloud Functions arc
        ruby:
          status: not-planned
          reason: not planned for the canonical GCP Cloud Functions arc
        php:
          status: not-planned
          reason: not planned for the canonical GCP Cloud Functions arc
  azure:
    function:
      azure-functions:
        python:
          status: supported
          support_level: first-class-example
          family: canonical-azure-functions
          example: blob-trigger-receipt-ocr
        typescript:
          status: supported
          support_level: first-class-example
          family: canonical-azure-functions
          example: cosmos-changefeed-search-sync
        dotnet-isolated:
          status: supported
          support_level: first-class-example
          family: canonical-azure-functions
          example: servicebus-classification
        powershell:
          status: deferred
          reason: PowerShell remains reserved for ops-only Azure Functions cases
