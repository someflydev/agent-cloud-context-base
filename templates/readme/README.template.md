# {{repo_name}}

Status: generated starter for a {{provider}} {{runtime_tier}} service using {{primary_language}} and {{iac_tool}}. Expand this README only after the repo has earned its shape.

Boot: read `AGENT.md`, then run `python3 scripts/work.py resume`.
