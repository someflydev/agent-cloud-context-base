Add the first IaC isolation pair.

Declare dev state `{{state_backend_dev}}`, test state `{{state_backend_test}}`, dev secrets `{{secret_path_dev}}`, test secrets `{{secret_path_test}}`, and resource names `<{{repo_slug}}>-<env>-<role>` before creating resources.
